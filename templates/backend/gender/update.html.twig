{% extends 'base.html.twig' %}

{% block title %}
    Modification d'un Genre
     {{ parent() }}
{% endblock %}

{% block body %}
    <section class="container mt-2">
        <h1 class="text-center">Modification d'un Genre</h1>

        {{ form_start(form, {attr:{class:'card w-75 max-auto'}} ) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-warning">Modifier</button>
        {{ form_end(form) }}
        <a href="{{ path('app.admin.gender.index') }}" class="btn btn-primary mt-2">Retour à la liste</a>
    </section>
{% endblock %}