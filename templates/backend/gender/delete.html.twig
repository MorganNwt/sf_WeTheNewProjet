<form action="{{ path('app.admin.gender.delete', {id:gender.id}) }}" method="POST" onsubmit="return confirm('Êtes vous sur de vouloir suprimer ce Genre')">
        <input type="hidden" name="token" value="{{ csrf_token('delete' ~ gender.id) }}">
        <button type="submit" class="btn btn-danger">Suprimer</button>
</form>